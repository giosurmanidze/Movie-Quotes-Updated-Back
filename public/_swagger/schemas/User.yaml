components:
  schemas:
    User:
      allOf:
        - $ref: "../common.yaml#/components/schemas/Id"
        # - type: object
        # properties:
          -type: object
           properties:
            email:
              type:string 
        - $ref: "../common.yaml#/components/schemas/TimeStamps"